import assert from "node:assert/strict";import {readFileSync} from "node:fs";import test from "node:test";import {inspectPortfolioSource} from "../src/modules/portfolio/portfolio-import.service.js";
const fixture=readFileSync(new URL("./fixtures/portfolio-synthetic.csv",import.meta.url));
test("validates and normalizes the BatFIN master portfolio source",()=>{const result=inspectPortfolioSource(fixture,"portfolio-synthetic.csv");assert.equal(result.errors.length,0);assert.equal(result.normalizedRowCount,4);assert.equal(result.accountCount,3);assert.equal(result.dealerCount,3);assert.ok(result.warnings.some(item=>item.includes("appears 2 times")));assert.equal(result.cases[0]?.loanId,"TESTLOAN001");assert.equal(result.cases[0]?.emi,5000);assert.equal(result.cases[0]?.disburseDate.toISOString().slice(0,10),"2025-08-28")});
test("source summary is treated only as reconciliation evidence",()=>{const result=inspectPortfolioSource(fixture,"portfolio.csv");assert.equal(result.sourceSummary?.contractedDemand,270000);assert.equal(result.sourceSummary?.delinquent,2);assert.equal(result.sourceSummary?.npaRepo,1);assert.equal(result.cases.some(item=>item.sourceRowNumber===669),false)});
