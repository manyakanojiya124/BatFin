import assert from "node:assert/strict";import {readFileSync} from "node:fs";import test from "node:test";import {inspectPortfolioSource} from "../src/modules/portfolio/portfolio-import.service.js";
const fixture=readFileSync(new URL("./fixtures/BatFIN_Portfolio_Summary_July26_Final.csv",import.meta.url));
test("validates and normalizes the BatFIN master portfolio source",()=>{const result=inspectPortfolioSource(fixture,"BatFIN_Portfolio_Summary_July26_Final.csv");assert.equal(result.errors.length,0);assert.equal(result.normalizedRowCount,664);assert.equal(result.accountCount,661);assert.equal(result.dealerCount,72);assert.ok(result.warnings.some(item=>item.includes("appears 2 times")));assert.equal(result.cases[0]?.loanId,"BATBR202508300001");assert.equal(result.cases[0]?.emi,5599);assert.equal(result.cases[0]?.disburseDate.toISOString().slice(0,10),"2025-08-28")});
test("source summary is treated only as reconciliation evidence",()=>{const result=inspectPortfolioSource(fixture,"portfolio.csv");assert.equal(result.sourceSummary?.contractedDemand,50396055);assert.equal(result.sourceSummary?.delinquent,32);assert.equal(result.sourceSummary?.npaRepo,5);assert.equal(result.cases.some(item=>item.sourceRowNumber===669),false)});
