import test from "node:test";import assert from "node:assert/strict";import {createSemanticModelSchema} from "../src/modules/admin-analytics/analytics-semantic.schemas.js";import {createDashboardSpecificationSchema} from "../src/modules/admin-analytics/analytics-dashboard.schemas.js";
const semantic={schemaVersion:1 as const,title:"Test model",datasetSummary:"A valid dataset summary.",businessDomain:"Test",identifiers:[],dimensions:[{field:"category",label:"Category",kind:"CATEGORY" as const,description:"Category"}],measures:[{id:"row_count",field:null,label:"Rows",kind:"ROW_COUNT" as const,aggregation:"COUNT" as const,format:"NUMBER" as const,description:"Rows"},{id:"amount",field:"amount",label:"Amount",kind:"FIELD" as const,aggregation:"SUM" as const,format:"NUMBER" as const,description:"Amount"}],dateFields:[{field:"date",label:"Date",type:"DATE" as const,description:"Date"}],suggestedFilters:[],calculatedMetrics:[]};
test("rejects unknown and nonnumeric semantic fields",()=>{const schema=createSemanticModelSchema([{field:"category",dataType:"STRING"},{field:"amount",dataType:"DECIMAL"},{field:"date",dataType:"DATE"}]);assert.equal(schema.safeParse(semantic).success,true);assert.equal(schema.safeParse({...semantic,measures:[{...semantic.measures[1],field:"category"}]}).success,false)});
test("validates dashboard widget registry and field references",()=>{const schema=createDashboardSpecificationSchema({semanticModel:semantic,allFields:["category","amount","date"]});const spec={schemaVersion:1 as const,title:"Dashboard",description:"Valid dashboard specification",filters:[],visualizations:[{id:"kpi_rows",type:"KPI" as const,title:"Rows",description:null,dimension:null,measure:"row_count",secondaryMeasure:null,aggregation:"COUNT" as const,stackBy:null,columns:[],limit:1,layout:{x:0,y:0,w:3,h:2},visible:true}],insights:[]};assert.equal(schema.safeParse(spec).success,true);assert.equal(schema.safeParse({...spec,visualizations:[{...spec.visualizations[0],measure:"unknown"}]}).success,false)});
