import{readFileSync}from"node:fs";import{basename,resolve}from"node:path";import{prisma}from"../src/database/prisma.js";import{importCustomerIdentities}from"../src/modules/portfolio/customer-identity-import.service.js";const path=resolve(process.argv[2]??"/home/user/uploads/lms068_collection_mis_18082026162029.csv");async function main(){const admin=await prisma.adminUser.findFirst({where:{role:"SUPER_ADMIN"},orderBy:{createdAt:"asc"}});const result=await importCustomerIdentities({buffer:readFileSync(path),fileName:basename(path),adminUserId:admin?.id??null});const job=result.importJob;console.log(`Customer identity import ${result.idempotent?"already applied":"completed"}: ${job.validIdentityCount} valid identities, ${job.createdUserCount} users created, ${job.linkedAccountCount} portfolio accounts linked, ${job.unmatchedLoanCount} unmatched loans.`)}main().catch(error=>{console.error("Customer identity import failed:",error instanceof Error?error.message:"Unknown error");process.exitCode=1}).finally(async()=>prisma.$disconnect());
