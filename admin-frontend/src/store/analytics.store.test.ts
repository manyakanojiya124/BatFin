import {beforeEach,describe,expect,it} from "vitest";import {useAnalyticsDashboardStore} from "./analytics.store";describe("analytics dashboard state",()=>{beforeEach(()=>useAnalyticsDashboardStore.getState().reset());it("persists and clears structured filters in memory",()=>{const store=useAnalyticsDashboardStore.getState();store.setDashboard("d1");store.setFilter("status",{field:"status",type:"SELECT",value:"Active"});expect(useAnalyticsDashboardStore.getState().filters.status?.value).toBe("Active");useAnalyticsDashboardStore.getState().setFilter("status",null);expect(useAnalyticsDashboardStore.getState().filters.status).toBeUndefined()})});
