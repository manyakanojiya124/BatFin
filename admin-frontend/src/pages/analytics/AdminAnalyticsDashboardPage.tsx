import {RefreshCw} from "lucide-react";import {useEffect,useState} from "react";import {useLocation,useParams} from "react-router-dom";import {DashboardShell} from "../../components/analytics/DashboardShell";import {getAnalyticsDashboard} from "../../services/api";import {useAdminStore} from "../../store/admin.store";import type {AnalyticsDashboardDetail} from "../../types/analytics";
export function AdminAnalyticsDashboardPage(){const{id}=useParams();const location=useLocation();const csrf=useAdminStore(s=>s.csrfToken);const[detail,setDetail]=useState<AnalyticsDashboardDetail|null>(null);const[error,setError]=useState('');useEffect(()=>{if(!id)return;void getAnalyticsDashboard(id).then(r=>setDetail(r.dashboard)).catch(e=>setError(e instanceof Error?e.message:'Unable to load dashboard'))},[id]);if(!detail||!csrf)return <main className="grid min-h-[70vh] place-items-center">{error?<p className="text-error">{error}</p>:<RefreshCw className="size-8 animate-spin text-primary"/>}</main>;return <main className="px-4 py-7 sm:px-6 lg:px-8 lg:py-10"><div className="mx-auto max-w-[1600px]"><DashboardShell csrfToken={csrf} initial={detail} initialEdit={location.pathname.endsWith('/edit')}/></div></main>}
