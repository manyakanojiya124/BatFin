import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it,vi} from "vitest";import {KPIWidget} from "./KPIWidget";import {TableWidget} from "./TableWidget";import {VisualizationEditor} from "./VisualizationEditor";import type {DashboardVisualizationSpec,SemanticModel} from "../../types/analytics";
describe("analytics renderer components",()=>{it("renders real KPI values",()=>{render(<KPIWidget rowCount={614} title="Portfolio" value={32127936}/>);expect(screen.getByText("Portfolio")).toBeInTheDocument();expect(screen.getByText(/614 matching records/)).toBeInTheDocument()});it("renders paginated table fields",()=>{render(<TableWidget labels={{account:"Account"}} result={{mode:"TABLE",fields:["account"],rows:[{rowNumber:1,data:{account:"A-1"}}],pagination:{page:1,pageSize:25,total:1,totalPages:1}}}/>);expect(screen.getByText("Account")).toBeInTheDocument();expect(screen.getByText("A-1")).toBeInTheDocument()});it("edits visualization without AI",()=>{const item:DashboardVisualizationSpec={id:"chart",type:"BAR",title:"Old title",description:null,dimension:"category",measure:"amount",secondaryMeasure:null,aggregation:"SUM",stackBy:null,columns:[],limit:20,layout:{x:0,y:0,w:6,h:4},visible:true};const semantic:SemanticModel={schemaVersion:1,title:"Model",datasetSummary:"Summary",businessDomain:"Test",identifiers:[],dimensions:[{field:"category",label:"Category",kind:"CATEGORY",description:"Category"}],measures:[{id:"amount",field:"amount",label:"Amount",kind:"FIELD",aggregation:"SUM",format:"NUMBER",description:"Amount"}],dateFields:[],suggestedFilters:[],calculatedMetrics:[]};const apply=vi.fn();render(<VisualizationEditor columns={[{normalizedName:"category",originalName:"Category",displayName:"Category",dataType:"STRING",semanticType:"CATEGORY",nullable:false}]} item={item} onApply={apply} onClose={()=>undefined} semantic={semantic}/>);fireEvent.change(screen.getByLabelText("Title"),{target:{value:"New title"}});fireEvent.click(screen.getByText("Apply without AI"));expect(apply).toHaveBeenCalledWith(expect.objectContaining({title:"New title"}))})});
